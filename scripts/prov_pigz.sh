#!/usr/bin/env bash
# This script has been deprecated.
# Provenance (prov-*) debug tooling has been removed from the project.
# Use the CLI logging options instead: `zlib-cli log-on` / `zlib-cli log-off`.
# Exiting with an error to signal deprecation.

echo "[DEPRECATED] scripts/prov_pigz.sh: provenance feature has been removed."
echo "Use CLI commands 'log-on' and 'log-off' to control logging."
exit 2

